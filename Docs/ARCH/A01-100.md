# Antigravity Root Architecture Change Logs (A01 - A100)

This document tracks system-level architecture and root workspace changes.

# [A-44] Fix Parent Category Label Resolution & Backend Entity Image Upload
## Change Summary
- **Module**: Categories & Asset Controller (`CategoryController.js`, `ImagesController.js`, `categories.jsx`, `category-cache.js`)
- **Impact**: UI Legibility, Data Integrity & Clean Entity Separation
- **Description**: Resolved an issue where parent category hex IDs/DIDs were displayed in the dropdown trigger instead of human-readable category names. Standardized backend `ImagesController.js` to branch entity uploads cleanly by type (`product`, `category`, `brand`) preserving aspect ratios (`fit: 'inside'`).

---

# [A-43] Standardize Gallery Images to String Array & Add Upload Toast Loader
## Change Summary
- **Module**: Product Models, Controllers & Dashboard Forms (`product.model.js`, `ProductsController.js`, `addNewProduct.jsx`, `productDetails.jsx`)
- **Impact**: Schema Purity, UX Feedback & Two-Phase Upload Protection
- **Description**: Simplified product `images` schema into an array of string URLs (`[String]`). Synchronized dashboard form payloads to submit clean string URL arrays. Added a two-phase upload guard with a persistent loading toast (`Uploading product images...`), ensuring product create/update APIs are only called after all asset uploads succeed.

---

# [A-42] Rename Product Creation File to addNewProduct.jsx
## Change Summary
- **Module**: Dashboard Routing & Product Pages (`addNewProduct.jsx`, `router.jsx`)
- **Impact**: Naming Consistency & Structural Alignment
- **Description**: Formally renamed `newProduct.jsx` to `addNewProduct.jsx` to match the component entity `AddNewProduct` and updated route references across `router.jsx`.

---

# [A-41] Purge Legacy Root Page Files, Fix Gallery Persistence & Align Header
## Change Summary
- **Module**: Dashboard Pages & Backend Controllers (`pages/`, `newProduct.jsx`, `productDetails.jsx`, `ProductsController.js`)
- **Impact**: Codebase Cleanliness, Asset Storage Integrity & UX Consistency
- **Description**: Purged 19 obsolete monolithic page templates from `dashboard/src/pages/` to eliminate structural ambiguity. Fixed gallery image saving and loading in product controllers and form handlers. Renamed page title to 'Add New Product'.

---

# [A-40] Fix Object-to-Primitive Sorting Exception in Product Listing API
## Change Summary
- **Module**: Backend Product Query Services (`backend/src/utils/productUtils.js`, `ProductsController.js`)
- **Impact**: API Stability & Listing Resilience
- **Description**: Resolved a `TypeError: Cannot convert object to primitive value` thrown when `buildProductSort` received raw query option objects. Enhanced `buildProductSort` to gracefully extract `sortBy`, `order`, and preset sort strings (`newest`, `oldest`, `price-asc`, `price-desc`).

---

# [A-39] Update Client Onboarding Guide with Dynamic Makefile Workflows
## Change Summary
- **Module**: Multi-Tenant Documentation & Setup Tooling (`Docs/02_Onboard_Client.md`, `client-setup.sh`)
- **Impact**: Operational Clarity & Standardized Tenant Provisioning
- **Description**: Updated the onboarding reference guide to reflect dynamic Makefile deployment targets, explicit client config path definitions (`CLIENT_CONFIG_PATH`), and environment variable best practices for Decantre and Engulfic.

---

# [A-38] Dynamic Multi-Tenant Deployment Automation in Makefile
## Change Summary
- **Module**: Infrastructure & Deployment Automation (`Makefile`)
- **Impact**: Multi-Tenant VPS Deployment Reliability
- **Description**: Re-architected `Makefile` to auto-detect client runtime environment files (`/opt/<client>/configs/backend.env`) or accept dynamic `CLIENT=<name>` parameters. Added granular targets (`make deploy`, `make build-backend`, `make build-dashboard`) eliminating fragile root `.env` dependencies.

---

# [A-37] Add Localhost Port 8005 to Backend CORS Allowed Origins
## Change Summary
- **Module**: Backend Server Application (`backend/src/app.js`)
- **Impact**: Multi-Tenant Local Development & Image Upload Stability
- **Description**: Added `http://localhost:8005` to default CORS origins in `app.js` to ensure seamless local API interactions and file uploads across the Engulfic local dashboard instance.

---

# [A-36] Style Discard Action with Destructive Feedback & Move SEO Metadata to Sidebar
## Change Summary
- **Module**: Dashboard Product Forms (`newProduct.jsx`, `productDetails.jsx`)
- **Impact**: Form Layout Ergonomics & Action Semantics
- **Description**: Re-styled Discard action buttons with prominent destructive alert cues. Relocated the Search Engine Optimization (SEO) metadata card into the right-hand sidebar column to declutter primary product configuration workflows.

---

# [A-35] Reposition Add New Variation Button to End of Variation List
## Change Summary
- **Module**: Dashboard Product Data Card (`ProductDataCard.jsx`)
- **Impact**: UX Navigation & Layout Cleanliness
- **Description**: Relocated the creation trigger button labeled 'Add New Variation' to the end of the variation cards list, simplifying top toolbar controls and following intuitive vertical form flow.

---

# [A-34] Add Client Feature Flag for Product Variation Image Uploads
## Change Summary
- **Module**: Multi-Tenant Client Config & Product Forms (`01decantre`, `02engulfic`, `03toyoland`, `ProductDataCard.jsx`)
- **Impact**: Tenant-Specific UI Customization
- **Description**: Added `features.variantImage` configuration support across client configs (`true` for Decantre, `false` for Engulfic and Toyoland). Variation rows in ProductDataCard now conditionally display image upload slots strictly when enabled by the active client tenant.

---

# [A-33] Enhance Variation Toolbar Alignment, Nested Card Depth, and Uniform Pricing
## Change Summary
- **Module**: Dashboard Product Data Card (`ProductDataCard.jsx`)
- **Impact**: UX Modularity, Apparel Pricing Ergonomics & Visual Contrast
- **Description**: Re-aligned the Variation Type toolbar into a horizontal inline control group with preset triggers. Added an 'Apply Same Price' toggle that sets global uniform pricing across all variation rows while disabling individual price inputs. Enhanced nested variation cards with distinct background contrast and structured header bands.

---

# [A-32] Fix Manual Slug Input Hyphen Character Truncation
## Change Summary
- **Module**: Dashboard Form Controls (`attributes.jsx`, `AttributesPage.jsx`, `ProductDetailsCard.jsx`)
- **Impact**: UX & Input Accuracy
- **Description**: Resolved an issue where typing a hyphen `-` in manual slug input fields was prematurely stripped by real-time slugify regex. Introduced `formatTypingSlug` during live typing while deferring end-trimming sanitization to blur/submit handlers.

---

# [A-31] Add 2-Column Slug Management for Attribute Variations
## Change Summary
- **Module**: Dashboard Attributes (`attributes.jsx`, `AttributesPage.jsx`)
- **Impact**: Variation Granularity & SEO-friendly Attribute Slugs
- **Description**: Upgraded the Attribute Values / Variations creation section into a 2-column input layout (Value Name and Value Slug) with auto-generation and manual override controls before appending variation chips.

---

# [A-30] Add File Size Validation for Gallery Image Uploads
## Change Summary
- **Module**: Dashboard Product Forms (`newProduct.jsx`, `productDetails.jsx`)
- **Impact**: Upload Security & Payload Optimization
- **Description**: Added a 2MB max image size restriction to gallery image uploads on both Add and Edit product pages, displaying informative toast warnings for oversized images and filtering only eligible images up to the 5-image ceiling.

---

# [A-29] Add Interactive Tag Management Card to Product Sidebar
## Change Summary
- **Module**: Dashboard Product Forms (`SidebarCards.jsx`, `newProduct.jsx`, `productDetails.jsx`)
- **Impact**: Product Categorization & Search Optimization
- **Description**: Added a dedicated Tags card in the right sidebar featuring an autocomplete/searchable tag input, chip badge rendering with deletion actions, keyboard shortcuts (Enter/Comma to submit), and a frontend enforcement limit of 10 tags per product.

---

# [A-28] Transition Product Status to Boolean isActive and Improve Form Typography
## Change Summary
- **Module**: Backend Schemas, Controllers, and Dashboard Products
- **Impact**: Simplified Storefront Querying, Soft Delete Compatibility & Form UX
- **Description**: Replaced multi-status strings with a clean boolean `isActive` flag across Mongoose schemas, controllers, and dashboard components. Added interactive status switches, unselected accordion empty state placeholders, increased label-to-input breathing room, and renamed `NewProductPage` to `AddNewProduct`.

---

# [A-27] Verify Dashboard and Backend Payload Integrity
## Change Summary
- **Module**: Verification & Build Validation
- **Impact**: Code Quality & System Stability
- **Description**: Verified modular dashboard build output with zero errors across all 3,321 modules and verified payload alignment across frontend dispatch and backend schema validation.

---

# [A-26] Add Product Schema Support for Tax Rates, Status, and SEO Metadata
## Change Summary
- **Module**: Backend Database & Controllers (`product.model.js`, `ProductsController.js`)
- **Impact**: Database Persistence & API Payload Integrity
- **Description**: Added schema support and controller lifecycle persistence for `taxRate`, `status`, and `metaData` objects on product documents while eliminating obsolete barcode mappings.

---

# [A-25] Refactor Edit Product Page with Modular Components and Revised UI Layout
## Change Summary
- **Module**: Dashboard Products (`productDetails.jsx`)
- **Impact**: Code Maintainability & UI Workflow
- **Description**: Rebuilt `EditProductPage` using modular sub-components, reducing component footprint from ~1200 lines down to ~400 lines while retaining full parity with the updated Add Product layout and existing data loading/updating pipelines.

---

# [A-24] Refactor Add Product Page with Modular Components and Revised UI Layout
## Change Summary
- **Module**: Dashboard Products (`newProduct.jsx`)
- **Impact**: Code Maintainability & UI Workflow
- **Description**: Rebuilt `NewProductPage` using modular sub-components, reducing component footprint from ~1000 lines down to ~350 lines. Implemented accordion-based product type selection, row-based variation layout, tax controls, and SEO metadata support.

---

# [A-23] Create Modular Dashboard Product Form Components
## Change Summary
- **Module**: Dashboard UI Architecture (`dashboard/src/components/dashboard/products/*`)
- **Impact**: Code Maintainability & Modularity
- **Description**: Created isolated sub-components (`ProductDetailsCard`, `ProductDataCard`, `SEOOverviewCard`, `SidebarCards`) to modularize product creation and edition workflows, decoupling heavy form logic and improving UI reusability.

---

# [A-22] Add Client-Specific Startup Scripts & Custom Branding Logos
## Change Summary
- **Module**: Project Run Configuration & Assets (`package.json`, logo loader)
- **Impact**: Development / Build Operations and branding assets.
- **Description**: Add client-specific NPM scripts (`dev:decantre`, `dev:engulfic`, `dev:toyland`) in `package.json` utilizing Vite modes. Custom client-specific logos will be integrated and served based on the active client.

---

# [A-21] Integrate Dynamic Client Menus Filtering in Sidebar
## Change Summary
- **Module**: Dashboard Navigation (`appSidebar.jsx`)
- **Impact**: Dashboard Navigation UI/UX
- **Description**: Refactored the dashboard sidebar to dynamically parse and filter top-level and sub-menu navigation links based on permissions set in the client's JSON configuration.

---

# [A-20] Create Client Route Guard for URL Access Control
## Change Summary
- **Module**: Client Routing Protection (`clientRouteGuard.jsx`)
- **Impact**: Dashboard Route Security
- **Description**: Created a route guarding mechanism to inspect page access permissions at the router level. Prevents clients from manually visiting disallowed paths via the browser address bar.

---

# [A-19] Implement Dynamic Client Theme Provider
## Change Summary
- **Module**: Styling / Design System (`clientThemeProvider.jsx`)
- **Impact**: UI Theme & Visual Branding
- **Description**: Created a runtime provider that reads theme colors from the client's JSON config and injects them as CSS variables onto the document root.

---

# [A-18] Establish Client Configuration Structure
## Change Summary
- **Module**: Configuration Directory (`src/clientConfig/*`)
- **Impact**: Client-wise Dashboard Configuration
- **Description**: Created configuration folders starting with two-digit identifiers using camelCase names (e.g. `01decantre`, `02engulfic`, `03toyland`) holding theme JSON files and access configurations. Resolved active client dynamically using the `VITE_CLIENT` environment variable.

---

# [A-17] Audit API Documentation and Capitalize File Titles
## Change Summary
- **Module**: Backend Documentation (`backend/docs/api/*`)
- **Impact**: Developer API Specifications
- **Description**: Capitalized the first letter of all API markdown filenames inside `backend/docs/api/`. Conducted an audit and created missing API spec documents for `/users`, `/billing`, and `/brands` endpoints.

---

# [A-16] Unify Architecture Docs & Reorganize Root Files
## Change Summary
- **Module**: System Documentation (`Docs/00_Architecture.md`, `Docs/03_Image-upload-pipeline.md`)
- **Impact**: Developer Onboarding & Context clarity
- **Description**: Unified all spread architectural and multi-client code isolation guidelines directly into a single master document `Docs/00_Architecture.md`. Cleaned up duplicate docs files and renamed the image upload pipeline to maintain naming sequence.

---

# [A-14] Create Database Migration Script for Order Schema Transition

## Change Summary

- **Module**: Backend Database Script (`backend/src/core/scripts/migrateOrderSchema.js`)
- **Impact**: Database Data Integrity / Legacy compatibility
- **Description**: Wrote a MongoDB update-aggregation script that migrates all existing order documents in the `orders` collection, renaming `customer` to `billingInfo`, `shippingAddress` to `shippingInfo`, and stripping any legacy `giftWrap` flags.

---

# [A-13] Update Order Email Dispatch & HTML Invoice Templates

## Change Summary

- **Module**: Backend Email Dispatch (`backend/src/core/utils/orderDelivery.js`, templates)
- **Impact**: Customer & Admin Order Notification Emails
- **Description**: Refactored `orderDelivery.js` to extract and resolve customer data using `billingInfo` and `shippingInfo`. Updated `orderInvoiceEmailTemplate.js` and `adminOrderEmailTemplate.js` layout variables to render structured billing & delivery details.

---

# [A-12] Update Backend Order Controllers and Controller Helpers

## Change Summary

- **Module**: Backend Business Logic (`OrdersController.js`, `orderControllerHelper.js`)
- **Impact**: Order Creation, Read & Filter Actions
- **Description**: Refactored `buildOrderDocument` helper to assemble order objects using the new nested `billingInfo` and `shippingInfo` models instead of flat customer properties. Updated search filters in `OrdersController` to query `billingInfo.email`.

---

# [A-11] Refactor Order Database Schema Model

## Change Summary

- **Module**: Backend DB Schema (`backend/src/core/models/order.model.js`)
- **Impact**: Backend Architecture Core
- **Description**: Refactored `OrderModel` schema. Replaced `customerSchema` with `billingInfoSchema` (removed redundant `city`, `giftWrap`). Replaced unstructured `shippingAddress: Mixed` with proper `shippingInfoSchema` containing structured validation rules (fullName, phone, address, district, thana, zip).

---

# [A-10] Deploy Toyoland Dev Frontend and Update Deployments Directory

## Change Summary

- **Module**: Deployment / Infrastructure (`Docs/z_deployments.md`)
- **Impact**: Client Frontend Release
- **Description**: Compressed and transferred the local Toyoland Next.js frontend code to `/ikram/website` on the VPS (`144.79.218.8`). Set up `.env` build args pointing to port `5092`, updated container mapping to expose port `8006` publicly, and deployed.

## Affected Files

- `Docs/z_deployments.md` — Modified

---

# [A-09] Create Toyoland Dev VPS Documentation and Update Deployments Directory

## Change Summary

- **Module**: Documentation (`Docs/Toyoland/VPS_Management.md`, `Docs/z_deployments.md`)
- **Impact**: Knowledge Base / Client Management
- **Description**: Created a dedicated VPS management guide for Toyoland Dev and fully refactored `Docs/z_deployments.md` to act as the unified master record for Decantre, Engulfic, and Toyoland Dev deployments (detailing all ports, VPS IPs, folder structures, and firewall commands).

## Affected Files

- `Docs/Toyoland/VPS_Management.md` — **NEW**
- `Docs/z_deployments.md` — Modified

---

# [A-08] Support dynamic config and upload paths in docker-compose.prod.yml

## Change Summary

- **Module**: Infrastructure (`docker-compose.prod.yml`)
- **Impact**: Deployment Flexibility / Config Management
- **Description**: Replaced hardcoded volume mount paths inside `docker-compose.prod.yml` with dynamic environment variables (having standard fallbacks). This allows custom upload and config paths per client (needed for Toyoland's custom `/ikram/caution/uploads` mapping).

## Affected Files

- `docker-compose.prod.yml` — Modified

---

# [A-07] Create Engulfic VPS Management Documentation

## Change Summary

- **Module**: Documentation (`Docs/Engulfic/VPS_Management.md`)
- **Impact**: Knowledge Base / Client Management
- **Description**: Created a detailed operational guide for managing the Engulfic production environment on the VPS (`144.79.218.8`). Covered folder structures, Nginx settings, port mapping, and redeployment steps.

## Affected Files

- `Docs/Engulfic/VPS_Management.md` — **NEW**

---

# [A-06] Expose MongoDB ports publicly on host for direct Compass access

## Change Summary

- **Module**: Infrastructure (`docker-compose.prod.yml`)
- **Impact**: Database Access / Operations
- **Description**: Modified the MongoDB service port binding to map to all host interfaces dynamically (using MONGODB_PORT variable) so the database can be connected directly from Mongo Compass. Security is enforced via VPS iptables firewall rules.

## Affected Files

- `docker-compose.prod.yml` — Modified

## Change Summary

- **Module**: Documentation (`Docs/Decantre/VPS_Management.md`)
- **Impact**: Deployment & Infrastructure Management
- **Description**: Created a comprehensive operational manual for managing the Decantre production environment on the VPS. Includes details on static file paths, `.env` management, docker volumes, container routing, and update commands.

## Affected Files

- `Docs/Decantre/VPS_Management.md` — **NEW**

---

# [A-04] Add volume mount for /var/www/uploads to /app/src/uploads in backend

## Change Summary

- **Module**: Infrastructure (`docker-compose.prod.yml`)
- **Impact**: File Storage / Asset Management
- **Description**: Added a volume mount to link `/var/www/uploads` from the VPS host directly into `/app/src/uploads` in the backend container, ensuring that images managed by `ImagesController` are preserved and accessible.

## Affected Files

- `docker-compose.prod.yml` — Modified

---

# [A-03] Remove MongoDB host port binding, use internal Docker network

## Change Summary

- **Module**: Infrastructure (`docker-compose.prod.yml`)
- **Impact**: Database Security / Port Conflicts
- **Description**: Removed the `127.0.0.1:27017:27017` port binding from the live mongodb container to resolve port conflicts with the dev mongodb instance on the VPS. The backend now connects to the database via internal docker network DNS.

## Affected Files

- `docker-compose.prod.yml` — Modified

# [A-02] Create Client Onboarding Documentation

## Change Summary

- **Module**: Documentation (`Docs/00_Onboard.md`)
- **Impact**: Knowledge Base / Client Onboarding
- **Description**: Created a detailed setup and onboarding guide for new white-label client instances on the VPS. Included config directory explanations, backend SMTP/CORS examples, dashboard environment settings, and step-by-step deploy instructions using the lowercase client argument setup.

## Affected Files

- `Docs/00_Onboard.md` — **NEW**

---

# [A-01] Fully White-Label Client Setup & Dynamic Compose Volume Mounting

## Change Summary

- **Module**: Root Configuration / Setup Script (`client-setup.sh`, `docker-compose.prod.yml`)
- **Impact**: Deployment & Infrastructure Management
- **Description**:
  1. Refactored `client-setup.sh` to accept a dynamic client name as a command argument (`bash client-setup.sh <client_name>`) instead of using a hardcoded `decantre` path.
  2. Updated the script to dynamically initialize client config storage at `/opt/<client_name>/configs` and uploads at `/opt/<client_name>/uploads`.
  3. Modified `docker-compose.prod.yml` to replace the hardcoded `/opt/decantre` paths with the dynamic environment variable `/opt/${CLIENT_NAME}`.
  4. Updated Docker Compose network identifiers and container volume bindings to use the dynamic setup.

## Affected Files

- `client-setup.sh` — Updated directories to use dynamic argument
- `docker-compose.prod.yml` — Switched volume paths to use `${CLIENT_NAME}` variable
