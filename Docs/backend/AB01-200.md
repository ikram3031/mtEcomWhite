# Backend Change Logs (AB01 - AB200)

This document tracks backend module changes, features, and API implementations.

# [AB92] Full API Documentation Synchronisation & Comprehensive Coverage
## Change Summary
- **Module**: Backend API Documentation & References (`backend/docs/api/*`, `backend/API_ENDPOINTS.md`, `Docs/backend/AB01-200.md`)
- **Impact**: Full documentation parity with backend codebase covering Reports, Media Storage Audit, Cloudflare R2 Sync, Dashboard Faceted Search, Slot Asset Manager, and Store Utilities Showcases.
- **Description**:
  1. **Reports API Documentation**: Created `24_Reports-api.md` covering summary KPI aggregations, sales timeline chart endpoints, top-selling product revenue calculations, payment method breakdowns, and real-time inventory valuations.
  2. **Media Audit & R2 Synchronization**: Created `25_Media-Audit-and-R2-Sync-api.md` detailing disk storage audits, filesystem vs database orphan candidate scans, Cloudflare R2 cloud backup sync, whitelisting, and orphan file purges.
  3. **Store Utilities Showcases**: Created `26_Store-Utils-api.md` covering retrieval and management of Featured and Best Seller product showcases with tag-based auto fallbacks.
  4. **Product Catalog & Faceted Search**: Updated `11_products_api.md` with `/api/v1/dash/products` deep faceted aggregation queries and recursive subcategory resolution.
  5. **Media & Asset Management**: Updated `02_Assets.md` to document Dashboard Slot Asset uploads (`/api/v1/dash/assets`) with <= 230KB WebP adaptive compression, and `05_images-api.md` with 10MB limit and media file listing (`GET /api/v1/images`).
  6. **Interactive Developer Reference**: Updated `19_System-and-Developer-api.md` with Scalar OpenAPI 3.0 interactive documentation (`GET /api/v1/developer/docs`) and updated `backend/API_ENDPOINTS.md` master index.

---

# [AB91] Persistent Contact Messages, Direct SMTP Replies and Unified Live Notifications
## Change Summary
- **Module**: Backend Contact & Messages System, Email Delivery, Logs & WebSocket (`backend/src/models/contactMessage.model.js`, `backend/src/models/log.model.js`, `backend/src/controllers/ContactController.js`, `backend/src/routes/ContactRoute.js`, `backend/src/utils/contactEmailDelivery.js`, `backend/src/controllers/LogsController.js`, `backend/src/websocket.js`)
- **Impact**: Persistent customer contact storage in MongoDB, direct dashboard email replies via SMTP with threaded history, elimination of admin submission spam emails, and real-time unified dashboard live notifications
- **Description**:
  1. **Persistent Contact Message Model**: Created `contact_messages` collection with DID tracking, status states (`unread`, `read`, `replied`), customer contact metadata, and nested conversation reply threads.
  2. **Public Submission Refactor**: Updated `POST /api/v1/contact` to persist submissions in DB, dispatch customer acknowledgment email, and create real-time `contactMessage` log notification. Eliminated admin email dispatch on submission per business requirements.
  3. **Admin Management & SMTP Direct Reply**: Built protected endpoints for listing (`GET /messages`), reading with auto-mark-read (`GET /messages/:id`), direct emailing with formatted HTML quoting original message (`POST /messages/:id/reply`), status toggling (`PATCH /messages/:id/status`), and soft deletion (`DELETE /messages/:id`, `POST /messages/bulk-delete`).
  4. **Unified Live Notifications**: Extended `LogModel`, `websocket.js`, and `LogsController.js` to index, query, and broadcast both `newOrder` and `contactMessage` events to active dashboard connections in real time.

---

# [AB90] Recursive Subcategory Resolution in Product Filter
## Change Summary
- **Module**: Backend Product Query Builder (`backend/src/utils/productUtils.js`)
- **Impact**: Single-request category filtering that automatically includes all child and subcategory products without frontend multi-call workarounds
- **Description**:
  1. **Recursive Subcategory Resolution**: Updated `buildProductFilter` so when querying products by category slug, DID, or ID, it recursively queries and aggregates all descendant/child category ObjectIds (`parent: { $in: currentParentIds }`).
  2. **Eliminated Partial Regex Duplication**: Removed ambiguous substring matching in category query to ensure clean exact matches and hierarchy-based product aggregation.

---

# [AB89] Meta Conversions API (CAPI) Server-Side Purchase Event Integration
## Change Summary
- **Module**: Backend Orders Controller, Environment Configuration & CAPI Service (`backend/src/config/env.js`, `backend/src/services/facebookCapi.service.js`, `backend/src/controllers/OrdersController.js`, `backend/.env`)
- **Impact**: 100% Reliable Server-Side Order Tracking with SHA-256 Hashed Identifiers and Test Event Code Support
- **Description**:
  1. **Environment & Schema**: Added `FB_PIXEL_ID`, `FB_ACCESS_TOKEN`, and `FB_TEST_EVENT_CODE` to backend `.env` and `src/config/env.js`.
  2. **CAPI Service**: Created `src/services/facebookCapi.service.js` with SHA-256 hashing for customer identifiers (`em`, `ph`, `fn`, `ln`, `ct`, `country` 'bd'), client IP / User-Agent detection, and event deduplication ID generation (`purchase_{orderNumber}`).
  3. **Non-Blocking Trigger**: Wired `sendServerPurchaseEvent` into `createOrder` controller alongside asynchronous order email notifications.

---

# [AB87] Cloudflare R2 Sync, Orphan Image Cleanup Scheduler & Admin Review APIs
## Change Summary
- **Module**: Backend Schedulers, Storage Services & Admin APIs (`backend/package.json`, `backend/src/config/r2.config.js`, `backend/src/models/mediaCleanupAudit.model.js`, `backend/src/models/r2SyncLog.model.js`, `backend/src/services/mediaAudit.service.js`, `backend/src/services/r2Sync.service.js`, `backend/src/schedulers/mediaScheduler.js`, `backend/src/dashboard/controllers/MediaAuditController.js`, `backend/src/dashboard/routes/mediaAuditRoute.js`, `backend/src/app.js`, `backend/src/server.js`)
- **Impact**: Zero Media Loss, Off-site Cloud Backup, Automated Storage Hygiene & Safe Admin Approval Workflow
- **Description**:
  1. **Package Version Bump**: Incremented minor version from `2.2.5` to `2.3.0` in `backend/package.json` and installed `@aws-sdk/client-s3`.
  2. **Cloudflare R2 Synchronizer**: Created S3-compatible R2 client helper and differential sync service (`r2Sync.service.js`) that compares local `/uploads` assets against Cloudflare R2 bucket keys and uploads missing files.
  3. **Orphan Media Scanner**: Implemented database reference aggregator (`mediaAudit.service.js`) scanning all active Products, Variants, Categories, Brands, and Meta documents to detect unused physical media on VPS disk without hard deletion.
  4. **Admin Approval & Review APIs**: Built protected endpoints under `/v1/api/admin/media-audit` (`/summary`, `/orphans`, `/scan`, `/r2-sync`, `/whitelist`, `/confirm`) enabling admins to view previews, whitelist false positives, and permanently delete approved orphan assets.
  5. **Background Schedulers**: Integrated cron/timer schedulers in `mediaScheduler.js` and hooked into server bootstrap and graceful shutdown in `server.js`.

---
