# Antigravity Dashboard Change Logs (ND01 - ND200)

This document tracks changes made to the `dashboard` project by Antigravity (ND). Entries are sorted by latest at the top. After the ND200 log, make new file (ND201-400.md)

### [ND78] Attribute Values UX: 2MB File Limit & Prominent Image Upload Dropzones

- **Description**: 
  1. **Increased File Size Limit to 2MB (`editAttribute.jsx`, `attributes.jsx`)**:
     - Increased maximum image file size validation from 1MB to 2MB (`2 * 1024 * 1024`).
     - Added support for `.webp` alongside `.jpg`, `.jpeg`, and `.png`.
     - Updated header badges to `1:1 Square Ratio • Max 2MB • JPG/PNG/WEBP`.
  2. **Prominent Image Upload Identifiers in Add Value Row**:
     - Redesigned the image upload trigger into a distinct, high-contrast dashed dropzone button with an upload icon and clear label.
     - Added preview box with clear file name and remove action when an image is staged.
  3. **Enlarged & Balanced Value Card Images (2-Column Grid)**:
     - Upgraded image box size from cramped 44px (`h-11 w-11`) to spacious 56px (`size-14` / `size-13`) with clean 2px dashed border and hover state.
     - Added rich hover overlay with delete action for existing images.
     - Improved typography and pill badge styling for attribute value slugs.
- **Affected Files**:
  - `dashboard/src/pages/dashboard/products/editAttribute.jsx`
  - `dashboard/src/pages/dashboard/products/attributes.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Solves unclear image dropzone affordances, allows 2MB image uploads, and delivers a balanced, professional card layout for attribute values.

---

### [ND77] All Media Preview Modal: Width Expansion & Enhanced Display Container

- **Description**: 
  1. **Expanded Dialog Width (`src/pages/dashboard/allMedia.jsx`)**:
     - Increased modal preview width from `max-w-xl` to `sm:max-w-3xl md:max-w-4xl` for a spacious, cinema-style asset inspection view.
  2. **Enhanced Image Preview Box**:
     - Upgraded image viewport from `max-h-72` to `min-h-[340px] max-h-[540px]` with `max-h-[500px]` image height.
     - Added an elegant, dark backdrop container (`bg-neutral-900/60 dark:bg-neutral-950/80 backdrop-blur-xs`) and clean rounded borders for a crisp display of transparent and card-based webp/png assets.
  3. **Direct Action Controls**:
     - Added direct `Open in New Tab` external link button alongside the primary `Copy Link` button.
- **Affected Files**:
  - `dashboard/src/pages/dashboard/allMedia.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Provides high-clarity full resolution image inspection with spacious, uncluttered preview layout.

---

### [ND76] All Media Image URL Resolution & VPS Dev Uploads Link

- **Description**: 
  1. **Image Base URL Resolution (`src/pages/dashboard/allMedia.jsx`, `src/lib/api-client.js`)**:
     - Exported `resolveImageUrl` from `api-client.js` to automatically prepend active backend `baseURL` to relative `/uploads/...` paths.
     - Updated `allMedia.jsx` to wrap thumbnail previews, full-size modal previews, and copied direct URLs with `resolveImageUrl`, fixing broken image icons in local development mode.
  2. **VPS Dev Uploads Directory Linked (`/dev/www/uploads`)**:
     - Replicated `/var/www/uploads` to `/dev/www/uploads` on VPS and mounted it to `devtest-backend` for isolated dev storage.
- **Affected Files**:
  - `dashboard/src/lib/api-client.js`
  - `dashboard/src/pages/dashboard/allMedia.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Restores full visual asset rendering across all media cards, preview modals, and copy links in local development.

---

### [ND75] Meta Catalog Feed Service: Audit, LocalStorage Persistence & In-Dropdown Category Search

- **Description**: 
  1. **LocalStorage Persistence (`src/pages/dashboard/tools/metaCatalog.jsx`)**:
     - Automatically saves staged feed products and custom feed configuration (store URL, prefix, image server base, ID mapping, condition, labels) to `localStorage`, preventing data loss across page reloads.
  2. **In-Dropdown Category Search**:
     - Added quick search input inside the category multi-select dropdown for fast filtering among large sets of categories/subcategories.
  3. **Select All Visible Action**:
     - Added a 1-click `Select All` / `Deselect All` toggle button to quickly select all currently loaded products in the grid.
  4. **Meta Commerce Standard Columns**:
     - Added standard `product_type`, `custom_label_0` to `custom_label_4`, and customizable product URL path prefix (`/products/` or `/product/`).
  5. **Fetch Race Condition Protection**:
     - Guarded requests with fetch count reference to prevent stale responses when rapidly changing category/stock filters.
- **Affected Files**:
  - `dashboard/src/pages/dashboard/tools/metaCatalog.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Production-grade Meta Commerce feed generator with persistent curation and enhanced search UX.

---

### [ND74] Dynamic API Base URL Support & Local Development Setup for Dev Backend

- **Description**: 
  1. **Dynamic Base URL Resolution (`src/lib/api-client.js`)**:
     - Updated `baseURL` to prioritize `import.meta.env.VITE_API_BASE_URL` before falling back to `clientConfig.apiBaseUrl` and default production URL.
  2. **Local Development Environment (`.env.development`)**:
     - Added `.env.development` configured with `VITE_API_BASE_URL=http://144.79.218.126:5092` so that running `npm run dev` locally automatically routes all API requests to the Dev backend.
- **Affected Files**:
  - `dashboard/src/lib/api-client.js`
  - `dashboard/.env.development`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Enables seamless local dashboard development directly connected to the isolated Dev backend.

---

### [ND73] Fix Meta Catalog Category Filter & Query Param Parsing

- **Description**: 
  1. **Category Parameter Serialization (`src/pages/dashboard/tools/metaCatalog.jsx`)**:
     - Fixed category query parameter mapping to pass comma-separated slug/DID/name strings (`params.category = catKeys.join(',')`), preventing empty results caused by nested array param serialization.
     - Improved category matching in `categoryOptions` and multi-select toggle.
  2. **Backend Category Query Resolution (`backend/src/utils/productUtils.js`)**:
     - Updated `buildProductFilter` to support comma-separated strings, `category[]`, case-insensitive regex name matching (`{ name: { $regex: ... } }`), and slug matching so selecting categories like "For Him", "For Her", "Unisex" accurately resolves product ObjectIds.
- **Affected Files**:
  - `backend/src/utils/productUtils.js`
  - `dashboard/src/pages/dashboard/tools/metaCatalog.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Instant, accurate category and subcategory filtering in the Meta Catalog feed builder.

---

### [ND72] Collapsed Sidebar Footer Minimal Mode (Profile & Logout Only)

- **Description**: 
  1. **Collapsed Footer Behavior (`src/components/dashboard/app-sidebar.jsx`)**:
     - Configured `group-data-[collapsible=icon]:hidden` on `All Media` and `Settings` action buttons.
     - When sidebar is in collapsed (icon-only) state, only the **Profile** and **Logout** icon buttons are displayed vertically, providing a clean, minimal footer layout.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Removes vertical clutter in collapsed icon mode while keeping essential profile and logout controls immediately accessible.

---

### [ND71] Toast Notification UX Optimization: Close Button & Single Visible Toast Limit

- **Description**: 
  1. **Dismissible Toasts with Close Button (`src/components/ui/sonner.jsx`)**:
     - Enabled `closeButton` on Sonner Toaster, rendering a cross (`✕`) button on the right side of every toast notification for manual dismissal.
  2. **Prevent Cluttered Toast Stacking**:
     - Configured `visibleToasts={1}` to ensure only one active notification is visible at a time instead of multiple stacking error messages cluttering the viewport.
     - Added `duration={3500}` for clean automatic dismissal.
- **Affected Files**:
  - `dashboard/src/components/ui/sonner.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Eliminates screen clutter from stacked error toasts with manual dismissibility and clean single-toast focus.

---

### [ND70] Sidebar Menu Items Visual Separation & Gap Spacing

- **Description**: 
  1. **Menu Item Vertical Spacing (`src/components/ui/sidebar.jsx`)**:
     - Increased menu item list separation from `gap-0` to `gap-1.5` in `SidebarMenu`.
     - Prevents consecutive active/hover items (like Accounting and Admin) from touching or visually overlapping, providing a clean, separated pill shape for each selected item.
- **Affected Files**:
  - `dashboard/src/components/ui/sidebar.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Clean, polished visual hierarchy with distinct spacing between all parent sidebar menu items.

---

### [ND69] Sidebar Navigation Renaming: Billing & Payment to Accounting

- **Description**: 
  1. **Menu Label & Tooltip Update (`src/components/dashboard/app-sidebar.jsx`)**:
     - Renamed parent navigation label and tooltip from `"Billing & Payment"` to `"Accounting"`.
     - Maintains all child routes (`Bills & Invoices`, `Payments`) and RBAC permissions cleanly in single line without layout breaking.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Shorter, cleaner, and more standard navigation terminology.

---

### [ND68] All Media Assets Viewer Page, Profile Green Border, and Footer Media Navigation

- **Description**: 
  1. **All Media Viewer Page (`src/pages/dashboard/allMedia.jsx` & `/dashboard/media`)**:
     - Created read-only media gallery for all authenticated roles.
     - **Grid Layout**: 5 items per row on desktop (20 cards total per page = 4 rows), sorted by latest uploaded first.
     - **Search & Pagination**: Top-right search bar with filename query filter and pagination.
     - **Media Detail Modal**: Clicking any card opens a preview modal with full file size, last modified date, and disabled direct link input with golden **Copy Link** action button.
  2. **Unified Translucent Outline Footer Action Icons (`src/components/dashboard/app-sidebar.jsx`)**:
     - **Profile Button**: Light green translucent theme (`bg-emerald-500/10 text-emerald-500 border-emerald-500/40`).
     - **All Media Button**: Light blue translucent theme (`bg-blue-500/10 text-blue-500 dark:text-blue-400 border-blue-500/40`) matching the circular outline design.
     - **Settings Button**: Golden translucent theme (`bg-amber-500/10 text-amber-500 dark:text-amber-400 border-amber-500/40`).
     - **Logout Button**: Red translucent theme (`bg-destructive/10 text-destructive border-destructive/40`).
- **Affected Files**:
  - `dashboard/src/pages/dashboard/allMedia.jsx` (NEW)
  - `dashboard/src/router.jsx` (MODIFIED)
  - `dashboard/src/components/dashboard/app-sidebar.jsx` (MODIFIED)
  - `dashboard/docs/ND01-200.md` (MODIFIED)
- **Impact**: Effortless asset previewing and quick URL copying across all system uploads for all staff members.

---

### [ND67] 404 Page Action Button Simplification & Themed Red Reload Button

- **Description**: 
  1. **Two-Button Action Bar (`src/components/NotFound.jsx`)**:
     - Streamlined the 404 Not Found fallback page to exactly 2 clear actions:
       - **Go Back**: Navigates directly to the root (`/`) route with primary golden theme accent.
       - **Reload**: Styled with a **light red background (`bg-destructive/10 hover:bg-destructive/20`)**, **red border (`border-destructive/40 hover:border-destructive/60`)**, and **red text (`text-destructive`)**, matching the logout theme.
- **Affected Files**:
  - `dashboard/src/components/NotFound.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Clean error recovery with visually distinct golden root navigation and red-themed reload action.

---

### [ND66] Logout Confirmation Alert Dialog with Custom Themed Actions & Fix Button Import

- **Description**: 
  1. **Logout Confirmation Dialog (`src/components/dashboard/app-sidebar.jsx`)**:
     - Clicking the sidebar footer logout button opens a confirmation dialog asking `"Do you want to log out?"`.
     - **Actions & Styling**:
       - **Yes Button**: Styled in vibrant **Golden (`bg-amber-500 hover:bg-amber-600 font-bold`)** to proceed with signing out.
       - **No Button**: Styled in **Red (`bg-destructive/10 hover:bg-destructive/20 text-destructive border-destructive/40`)** to cancel and stay on the dashboard.
     - Resolved missing `Button` component import in `app-sidebar.jsx` to prevent runtime error boundary triggers.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Prevents accidental session logouts with clear, color-coded confirmation actions.

---

### [ND65] Interactive Single-Open Accordion Sidebar, Light Golden Submenu Guide & Fixed System Logs

- **Description**: 
  1. **Single-Open Accordion Submenu Navigation (`src/components/dashboard/app-sidebar.jsx`)**:
     - All submenus are closed by default upon loading.
     - Clicking any parent menu (Orders, Products, Billing, Admin, Tools) expands its sub-items and automatically collapses any previously open menu.
     - Integrated animated right-pointing chevron arrows (`ChevronRight`) that rotate 90 degrees with a gold accent when expanded.
  2. **Light Golden Submenu Vertical Line (`src/components/ui/sidebar.jsx`)**:
     - Upgraded the submenu guide line from dull gray/ash to a subtle, elegant light golden border (`border-amber-500/30`).
  3. **Fixed Bottom Position for System Logs**:
     - Anchored `System Logs` at the fixed bottom of the navigation content with `mt-auto pt-2`, ensuring it never floats or shifts during submenu expansion.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/components/ui/sidebar.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Clean, uncluttered sidebar experience with smooth accordion toggling, on-brand golden guides, and consistent System Logs placement.

---

### [ND64] Real-Time Member Order Analytics & Spacious Profile Modal UI (Zero Mock Fallbacks)

- **Description**: 
  1. **Strict Database Analytics & Complete Mock Data Removal (`use-members.js` & `members-table.jsx`)**:
     - Completely removed hardcoded `mockMembers` arrays and synthetic fallback objects.
     - Entirely powered by live MongoDB API results; members with no orders accurately display `0` orders and `৳0.00` spent.
  2. **Spacious & Clean Member Profile Dialog (`members-table.jsx`)**:
     - Replaced inactive action toast with a rich, full-width modal (`max-w-2xl`).
     - Includes:
       - Large Avatar with photo upload badge placeholder.
       - Highlights grid: Total Orders, Lifetime Spent (৳), and Member Since.
       - Contact info (Email & Phone).
       - Saved Billing & Shipping addresses.
       - Recent order history snapshot with live order numbers and status badges.
- **Affected Files**:
  - `dashboard/src/components/dashboard/members-table.jsx`
  - `dashboard/src/hooks/use-members.js`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Accurately reflects member purchases/revenue and provides an organized, comprehensive customer profile view.

---

### [ND63] In-Store Orders Default Completed & Paid Status Synchronization

- **Description**: 
  1. **Order Creation Payload (`src/pages/dashboard/orders/newOrder.jsx`)**:
     - Configured in-store order creation to explicitly send `status: 'completed'`, `paymentStatus: 'paid'`, and `paidAmount: finalTotal`.
  2. **Payment Record Creation**:
     - Synced payment creation request with `status: 'paid'`, `paidAmount: finalTotal`, `pendingAmount: 0`, and `paymentStatus: 'paid'`.
- **Affected Files**:
  - `dashboard/src/pages/dashboard/orders/newOrder.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: All in-store walk-in sales are immediately finalized as `Completed` orders and marked `Paid` without manual status updates.

---

### [ND62] Sidebar Brand Version Display & Attributes Card Header UI Polish

- **Description**: 
  1. **Sidebar Brand Version Display (`src/components/dashboard/app-sidebar.jsx`)**:
     - Dynamically rendered the current dashboard version (`v3.2.2` from `package.json`) directly beneath the Decantre logo in the Sidebar Header.
  2. **Attributes Card Header & Action Buttons Polish (`src/pages/dashboard/products/attributes.jsx`)**:
     - Upgraded action icons into rounded-full buttons with smooth outline borders and active hover effects.
     - Enhanced value count badge and slug styling for unified visual hierarchy.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/pages/dashboard/products/attributes.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Provides instant visibility of active deployment version and cleaner card header aesthetics.

---

### [ND61] Product Attributes 3-Column Card Grid View, Full-Page Dedicated Editor & 1:1 Image Upload

- **Description**: 
  1. **Full-Width Responsive 3-Column Card Grid Layout (`src/pages/dashboard/products/attributes.jsx`)**:
     - Upgraded the attributes page from table view to a modern 3-column card grid (`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 w-full`).
     - Standardized equal card sizes with responsive inner value badges and text truncation with ellipsis (`truncate`) to prevent overflow.
  2. **Dedicated Full-Page Attribute Editor (`src/pages/dashboard/products/editAttribute.jsx`)**:
     - Clicking "Edit" navigates to a dedicated full-page editor at `/dashboard/products/attributes/:id`.
     - Configured values render in a clean **2-column grid (`grid-cols-1 md:grid-cols-2`)** with `bg-background` and subtle border/hover highlight to seamlessly match the application theme.
     - Per-value 1:1 square asset upload with instant preview and remove options.
  3. **Per-Value Image Upload & 1:1 Aspect Ratio Validation**:
     - Uploads separate images for each individual attribute value.
     - **Validation**: Enforced max file size $\le 1\text{ MB}$, allowed extensions (JPG, JPEG, PNG), and strict $1:1$ square aspect ratio checking with real-time feedback.
     - Images are optimized and stored directly in `/uploads/assets/attributes` via backend API.
- **Affected Files**:
  - `dashboard/src/pages/dashboard/products/attributes.jsx`
  - `dashboard/src/pages/dashboard/products/editAttribute.jsx` (NEW)
  - `dashboard/src/router.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Clean full-width UI, no cramped modals during edit, uniform card grids with ellipsis, and robust 1:1 image asset storage.

---

### [ND60] In-Store Order Creation Manual Discount Field & Dynamic Calculation

- **Description**: 
  1. **Manual Discount Input Field (`src/pages/dashboard/orders/newOrder.jsx`)**:
     - Added an inline `Discount (৳)` input field inside the Cart summary card.
     - Supports live instant amount deductions capped at the current cart subtotal.
  2. **Dynamic Live Calculations**:
     - Automatically renders applied discount line item (`- ৳...`) in green.
     - Live calculates `Total Payable = Math.max(0, subtotal - discountAmount)`.
  3. **Order & Payment Synchronization**:
     - Passed `discountTotalAmount: discountAmount` and `total: finalTotal` to `POST /api/v1/orders/new-order`.
     - Synced payment amount (`amount: finalTotal`) in `POST /api/v1/payments`.
     - Included accurate `discount` and `total` in customer invoice emails.
- **Affected Files**:
  - `dashboard/src/pages/dashboard/orders/newOrder.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Enables counter staff to grant manual discounts or price concessions seamlessly during walk-in sales with 100% financial and invoice accuracy.

---

### [ND59] Comprehensive 404 Not Found Fallback & Route Error Boundary Integration

- **Description**: 
  1. **Enhanced 404 Page Component (`src/components/NotFound.jsx`)**:
     - Polished responsive 404 presentation with centered status badge, primary-tinted icon container, and informative description.
     - Provided 3 instant navigation actions:
       - **Go Back**: `window.history.back()` to return to previously active page.
       - **Return to Dashboard**: Direct link back to `/dashboard`.
       - **Reload**: Refresh current state.
  2. **Double-Layer Catch-All & Fallback Routing (`src/router.jsx`)**:
     - Configured nested wildcard route (`/dashboard/* -> <NotFound />`) inside `DashboardLayout` so the sidebar and header remain intact when an invalid sub-path is entered.
     - Configured root wildcard route (`/* -> <NotFound />`) and `RouteErrorElement` boundaries across `/login`, `/invite`, and `/dashboard` to capture uncaught runtime exceptions and 404s gracefully.
- **Affected Files**:
  - `dashboard/src/components/NotFound.jsx`
  - `dashboard/src/router.jsx`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Zero broken/blank screens on invalid URLs or unauthorized routes with intuitive recovery actions.

---

### [ND58] Store Utilities Modular Settings Architecture & Showcase Curation UI

- **Description**: 
  1. **Modular Settings Folder Architecture (`src/pages/dashboard/settings/`)**:
     - Modularized settings into dedicated tab components:
       - `index.jsx`: Main settings page wrapper with tab state.
       - `FeaturedProductsTab.jsx`: Featured products curation tab.
       - `BestSellingProductsTab.jsx`: Best selling products curation tab.
       - `CatalogPreferencesTab.jsx`: Catalog display configurations.
       - `SecurityAccessTab.jsx`: Security and access policies.
       - `ProductShowcaseManager.jsx`: Reusable core showcase manager with live search, staging, and explicit save.
  2. **Decoupled Showcase Curation from Product Tags**:
     - Built `ProductShowcaseManager` connected to `/api/v1/store-utils`.
     - Completely decoupled `featured` and `bestSeller` showcases from product tags, avoiding any document mutation in the main products catalog.
  3. **TanStack Query Caching & Staging State**:
     - Caches store utility showcases via TanStack Query (`['store-utils']`).
     - Maintains local staging array (`stagedProducts`) so live search and additions appear in the table instantly.
  5. **Strict RBAC Enforcement (Owner, Admin, Manager Only)**:
     - `core.json`: Restricted `settings` permission exclusively to `Owner`, `Admin`, and `Manager` (`Marketing Expert` does not have access).
     - `app-sidebar.jsx`: Conditionally rendered Settings icon button via `isAllowed("settings")`.
     - `router.jsx`: Protected `/dashboard/settings` route via `<RoleGuard menuKey="settings">` (unauthorized access renders `<NotFound />`).
     - `StoreUtilsRoute.js`: Restricted `PUT /api/v1/store-utils` endpoint with `authorizeRoles("Owner", "Admin", "Manager")`.
- **Affected Files**:
  - `dashboard/src/pages/dashboard/settings/index.jsx` (NEW)
  - `dashboard/src/pages/dashboard/settings/ProductShowcaseManager.jsx` (NEW)
  - `dashboard/src/pages/dashboard/settings/FeaturedProductsTab.jsx` (NEW)
  - `dashboard/src/pages/dashboard/settings/BestSellingProductsTab.jsx` (NEW)
  - `dashboard/src/pages/dashboard/settings/CatalogPreferencesTab.jsx` (NEW)
  - `dashboard/src/pages/dashboard/settings/SecurityAccessTab.jsx` (NEW)
  - `dashboard/docs/ND01-200.md`
- **Impact**: Clean modular frontend code structure, zero accidental auto-updates, seamless TanStack Query caching, and clean, responsive product showcase curation.

---

### [ND57] Header User Profile Dropdown Cleanup, Sidebar Footer 2-Column Actions & Store Settings Page

- **Description**: 
  1. **Header User Profile Removal & Single Sidebar Anchor**:
     - Completely removed the top-right header avatar dropdown.
     - Kept only ThemeToggle and NotificationBell in the main header.
     - Profile details are exclusively anchored in the sidebar footer via the upward popover trigger.
  2. **Sidebar Width Optimization & 3-Icon Footer**:
     - Adjusted `SIDEBAR_WIDTH` to exactly `200px` in `src/components/ui/sidebar.jsx` for a sleeker dashboard layout.
     - Designed 3 rounded-full (`rounded-full`) action buttons at the sidebar footer:
       - **Left: Profile Icon Button**: Clicking displays an upward popover (`DropdownMenuContent side="top"`) showing the user's name, email, and highlighted role badge.
       - **Middle: Settings Icon Button**: Golden primary accented rounded button linking to `/dashboard/settings`.
       - **Right: Logout Icon Button**: Red icon (`text-destructive`), red border (`border-destructive/40`), and light red hover background (`bg-destructive/10`).
  3. **Dedicated Store Settings Page (`src/pages/dashboard/settings.jsx`)**:
     - Built horizontal top tab navigation for store settings.
     - **Tab 1: Featured Products**: Search and curate products with instant tag assignment (`featured`), table overview, and remove actions.
     - **Tab 2: Best Selling Products**: Search and curate products with instant tag assignment (`best-seller`), table overview, and remove actions.
     - **Tab 3: Catalog Display**: Placeholder for future automated collection sorting and display configurations.
     - **Tab 4: Security & Access**: Placeholder for advanced multi-tenant access policy and session management.
  4. **RBAC & Router Integration**:
     - Registered `/dashboard/settings` route in `router.jsx` protected with `RoleGuard`.
     - Added `settings` permission in `core.json` for role access validation.
- **Affected Files**:
  - `dashboard/src/components/dashboard/header.jsx`
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/pages/dashboard/settings.jsx` (NEW)
  - `dashboard/src/router.jsx`
  - `dashboard/src/config/core.json`
  - `dashboard/docs/ND01-200.md`
- **Impact**: Clean user header, intuitive 2-column sidebar actions, and dedicated product showcase settings.

---

### [ND56] Configuration-Driven RBAC (Marketing Expert Role) & User Invitation Onboarding

- **Description**: 
  1. **User Model & Roles Update**: Replaced `Employee` and `Editor` with `Marketing Expert` in `user.model.js`, validation helper, and invite modal.
  2. **Centralized RBAC Engine (`src/config/core.json` & `src/lib/rbac.js`)**:
     - Defined `userRoles` under `supportEmail` with strict parent-child menu access validation.
     - `Marketing Expert` granted exclusive access to `Tools` (and submenus) and `Products` (`Product List`, `Categories`, `Brands`, `Attributes`, `Coupons` — `Add Product` is blocked).
     - Protected routes with `RoleGuard` rendering `<NotFound />` (404 Page) on unauthorized access.
  3. **Sidebar Reorganization**: Moved `System Logs` out of `Tools` submenu to become a standalone main menu item placed directly under `Tools` (`/dashboard/logs`).
  4. **Dynamic User Invitation & Onboarding (`/invite`)**:
     - Generates encrypted invite tokens and dispatches responsive HTML invitation emails via SMTP.
     - Built onboarding portal (`src/pages/invite.jsx`) handling token verification, "Already Registered" status with login redirect, and strong password creation (8+ chars, upper, lower, number, special char).
- **Affected Files**:
  - `backend/src/models/user.model.js`
  - `backend/src/helper/userControllerHelper.js`
  - `backend/src/templates/userInviteEmailTemplate.js` (NEW)
  - `backend/src/controllers/UsersController.js`
  - `backend/src/controllers/AuthController.js`
  - `backend/src/routes/AuthRoute.js`
  - `dashboard/src/config/core.json` (NEW)
  - `dashboard/src/lib/rbac.js` (NEW)
  - `dashboard/src/components/NotFound.jsx` (NEW)
  - `dashboard/src/components/RoleGuard.jsx` (NEW)
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/components/dashboard/system-users-table.jsx`
  - `dashboard/src/pages/dashboard/users.jsx`
  - `dashboard/src/pages/invite.jsx` (NEW)
  - `dashboard/src/router.jsx`
- **Impact**: Enterprise-grade RBAC architecture and automated, secure team member onboarding.

---

### [ND55] Meta Catalog Feed Service: 5-Column Grid, Infinite Scroll & Selective Builder

- **Description**: 
  1. Added `Meta Catalog` (`/dashboard/tools/meta-catalog`) under the `Tools` section in `app-sidebar.jsx` and `router.jsx`.
  2. Redesigned `MetaCatalogGenerator` page component (`src/pages/dashboard/tools/metaCatalog.jsx`):
     - **5-Column Product Card Grid**: Clean, modern cards on desktop (2-5 responsive grid) with 200×200 square thumbnail, category name, title, price range (with variant counts), and instant Add/Remove toggle button.
     - **Infinite Scroll Engine**: Seamlessly fetches 20 items per page with `IntersectionObserver` sentinel, loading skeletons, and real total catalog count (`pagination.total`).
     - **Bulk Selection & Add**: Individual card checkboxes to multi-select items and bulk add via `Add Selected (X)` or `Add All (X)`.
     - **Category & Subcategory Multi-Filter**: Multi-select dropdown and chips for instant category filtering.
     - **In-Stock Default**: Automatically defaults to in-stock items with toggle for all/out-of-stock.
     - **Pricing Accuracy**: Real-time extraction of simple and variable product price ranges.
     - **Export**: Fixed `BDT` currency and 1-click download of Meta-compliant CSV feed.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/pages/dashboard/tools/metaCatalog.jsx` (NEW)
  - `dashboard/src/router.jsx`
- **Impact**: Highly responsive 5-column product card grid with infinite scroll and selective Meta feed staging.

---

### [ND54] Remove API Docs & Restore Realtime API Call / System Logs in Tools

- **Description**: 
  1. Removed `API Docs` menu item from `Tools` in `app-sidebar.jsx` and `router.jsx`.
  2. Built and restored dedicated real-time `SystemLogs` viewer (`src/pages/dashboard/tools/systemLogs.jsx`):
     - Real-time SSE (`/api/v1/developer/logs/stream`) & polling stream to track live incoming API requests.
     - Displays timestamp, HTTP method badges (GET, POST, PUT, DELETE), request path, status codes (200, 400, 500), response time in ms, and IP address.
     - Terminal console view with method filtering, search bar, pause/resume stream toggle, auto-scroll toggle, clear logs, and database backup download.
  3. Registered `/dashboard/tools/logs` and `/dashboard/developer/logs` paths in `router.jsx`.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/pages/dashboard/tools/systemLogs.jsx` (NEW)
  - `dashboard/src/router.jsx`
- **Impact**: Clean Tools section with Bulk Image Resize and dedicated real-time API request monitoring.

---

### [ND53] Rename "Developer Tools" to "Tools", Remove System Logs & Add Bulk Image Resize

- **Description**: 
  1. Renamed sidebar menu `Developer Tools` to `Tools` in `app-sidebar.jsx`.
  2. Completely removed `System Logs` menu item and deleted `systemLogs.jsx` page component.
  3. Added `Bulk Image Resize` (`/dashboard/tools/bulk-image-resize`) as the primary menu item under `Tools`.
  4. Built high-performance client-side `BulkImageResize` component (`bulkImageResize.jsx`) featuring:
     - E-commerce presets (Product Main 1200×1200, Product Thumb 600×600, Square HD 1080×1080, Banner 1920×1080, Custom).
     - Multi-image dropzone and queue with format conversion (WebP, JPEG, PNG) and quality slider.
     - 100% Client-Side HTML5 Canvas processing engine (zero server bandwidth consumption).
     - Single download and batch download as a `.ZIP` file using `JSZip`.
  5. Updated `router.jsx` to register `/dashboard/tools/bulk-image-resize` and redirect legacy developer paths.
  6. Updated client config `allowedMenus` with `tools`.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/pages/dashboard/tools/bulkImageResize.jsx` (NEW)
  - `dashboard/src/pages/dashboard/developer/systemLogs.jsx` (DELETED)
  - `dashboard/src/router.jsx`
  - `dashboard/src/clientConfig/01decantre/config.json`
  - `dashboard/src/clientConfig/02engulfic/config.json`
  - `dashboard/src/clientConfig/03toyoland/config.json`
- **Impact**: Streamlined Tools sidebar section with instant client-side bulk image resizing that consumes 0% server bandwidth.

---

### [ND52] Admin Reviews Menu & Status Approval UI Integration

- **Description**: 
  1. Positioned `Reviews` under the `Admin` section in the dashboard sidebar (`app-sidebar.jsx`) with route `/dashboard/reviews`.
  2. Enhanced `ReviewsTable` component (`reviews-table.jsx`):
     - Added distinct color-coded badges for approval status (Emerald Green for `Approved`, Amber for `Not Approved / Pending`).
     - Added 1-click `Switch` toggle directly in the table row to immediately approve or unapprove reviews with real-time optimistic state and toast feedback.
     - Added Star rating visual display, customer avatar initials, product info, and formatted date.
     - Added full review details modal dialog (`viewReview`).
  3. Created `ReviewsPage` at `src/pages/dashboard/reviews.jsx` with search filter, status filter, and bulk actions.
  4. Updated `router.jsx` to route `/dashboard/reviews` to `ReviewsPage` and redirected `/dashboard/products/reviews`.
  5. Updated client configurations (`01decantre`, `02engulfic`, `03toyoland`) `allowedMenus` list.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/components/dashboard/reviews-table.jsx`
  - `dashboard/src/pages/dashboard/reviews.jsx` (NEW)
  - `dashboard/src/router.jsx`
  - `dashboard/src/clientConfig/01decantre/config.json`
  - `dashboard/src/clientConfig/02engulfic/config.json`
  - `dashboard/src/clientConfig/03toyoland/config.json`
- **Impact**: Clean, dedicated Reviews management under the Admin tab with instant approval toggling and distinct status color indicators.

---

### [ND51] Product Reviews UI & Soft Delete Integration

- **Description**: 
  1. Added a new `Reviews` menu under the `Products` tab in the dashboard sidebar (`app-sidebar.jsx`).
  2. Created a dedicated Reviews data table (`reviews-table.jsx`) with actions to Approve/Unapprove and Delete reviews.
  3. Integrated `use-reviews.js` data fetching hook using TanStack query.
  4. Registered the `/dashboard/products/reviews` path in `router.jsx` and created the `reviews.jsx` page component.
  5. Reviews table automatically displays only `active: true` records per the updated backend schema enforcing soft deletion.
- **Affected Files**:
  - `dashboard/src/components/dashboard/app-sidebar.jsx`
  - `dashboard/src/components/dashboard/reviews-table.jsx` (NEW)
  - `dashboard/src/hooks/use-reviews.js` (NEW)
  - `dashboard/src/pages/dashboard/products/reviews.jsx` (NEW)
  - `dashboard/src/router.jsx`
- **Impact**: Full Dashboard UI capabilities to manage, approve, and remove product reviews.

---

### [ND50] Remove `core` Directory & Flatten Dashboard Architecture

- **Description**: 
  1. Removed nested `core/` directories from `src/components/`, `src/hooks/`, `src/lib/`, and `src/utils/`.
  2. Moved all core components, custom hooks, utilities, and helper functions directly into their parent directories (`src/components/`, `src/hooks/`, `src/lib/`, `src/utils/`).
  3. Refactored all `@/components/core/`, `@/hooks/core/`, `@/lib/core/`, and `@/utils/core/` import statements across all pages, layouts, and components.
  4. Resolved merge conflict markers and validated build via `npm run build`.
- **Affected Files**:
  - `dashboard/src/components/`
  - `dashboard/src/hooks/`
  - `dashboard/src/lib/`
  - `dashboard/src/utils/`
  - `dashboard/src/App.jsx`
  - `dashboard/src/router.jsx`
  - `dashboard/src/layouts/dashboardLayout.jsx`
- **Impact**: Codebase Simplification & Clean `@/` Path Resolution

---

### ND27: White-Label Dashboard Environment Configurations & Fallback API Base Cleanup

- **Description**: Removed all hardcoded client API endpoints and backend host URL references to white-label the dashboard service for split repository setups.
- **Affected Files**:
  - `dashboard/.env`
  - `dashboard/.env.local`
  - `dashboard/src/lib/api-client.js`
  - `dashboard/src/lib/core/api-client.js`
- **Impact**: Prepares the dashboard configuration to load backend API base URLs dynamically from the mounted `.env` config file without leaking fallback domains like `https://server.decantrebd.com`.

---

### ND26: Fix 404 Wildcard Routing, Functional Error Boundary, and Invoice Download URL

- **Description**: Configured catch-all wildcard routing (`*`) fallback to display a dedicated 404 error page. Refactored class-based `ErrorBoundary` to modern functional component with arrow syntax. Corrected invoice download link to point directly to the remote API base URL instead of relative dashboard route.
- **Affected Files**:
  - `dashboard/src/router.jsx`
  - `dashboard/src/components/core/ErrorBoundary.jsx`
  - `dashboard/src/components/core/dashboard/orders-table.jsx`
- **Impact**: Restores error boundary tracking, prevents broken 404 states, aligns text/icon buttons, and fixes PDF invoice exports from the orders table.

### ND25: Add Invoice Download Action and Relative Icons to Orders Table

- **Description**: Added Invoice action with download icon (`Download`) to the `OrdersTable` action dropdown menu. Enhanced all action menu items with intuitive Lucide icons (`Eye`, `Edit`, `RefreshCw`, `Download`, `Trash2`). Simplified menu labels for better readability (`Status`, `Delete`). Added `gap-1.5` spacing between main sidebar menu items.
- **Affected Files**:
  - `dashboard/src/components/core/dashboard/orders-table.jsx`
  - `dashboard/src/components/core/dashboard/app-sidebar.jsx`
- **Impact**: Enhances UX and action clarity in the Orders Management table and improves sidebar spacing.

### ND24: Collapsible Sidebar Submenus and Stock Management Tab Comment

- **Description**: Updated `AppSidebar` component to make submenus (`Orders`, `Products`, `Billing & Payment`, `Developer Options`) collapsible by default with rotating `ChevronRight` arrows. Added state management to toggle submenus individually and automatically expand the parent menu when navigating directly to a child route. Commented out the Stock Management tab per user instructions.
- **Affected Files**:
  - `dashboard/src/components/core/dashboard/app-sidebar.jsx`
- **Impact**: Improves sidebar navigation UX by reducing visual clutter and organizing menu hierarchies cleanly.

### ND23: Dashboard Header, Sidebar Theme and Order Status UI Refactor

- **Description**: Refactored the dashboard overview to display a fixed 30-day date range with dynamic date calculation. Redesigned `OrderStatusPie` chart using distinct status colors (green, blue, gold, red) and clean tabular legend. Updated top header and sidebar header to matching fixed heights (`h-16`) and dark backgrounds (`bg-zinc-950`) even in light mode. Configured collapsed sidebar to maintain a dark background across light and dark modes.
- **Affected Files**:
  - `dashboard/src/pages/dashboard/overview.jsx`
  - `dashboard/src/components/core/dashboard/OrderStatusPie.jsx`
  - `dashboard/src/components/core/dashboard/app-sidebar.jsx`
  - `dashboard/src/components/core/dashboard/header.jsx`
  - `dashboard/src/components/core/ui/sidebar.jsx`
- **Impact**: Improves visual consistency across light/dark modes and enhances UX for order pipeline tracking and header alignment.

### ND22: Range-Based KPI Hook Integration and Order Status Pie Chart

- **Description**: Integrated `useDashboardKpi` and `useOrderStatusDistribution` hooks to load dynamic dashboard data based on time range filters. Added an interactive `OrderStatusPie` chart component to show order status distribution.
- **Affected Files**:
  - `dashboard/src/components/core/dashboard/kpiCard.jsx`
  - `dashboard/src/pages/dashboard/overview.jsx`
  - `dashboard/src/components/core/dashboard/OrderStatusPie.jsx`
  - `dashboard/src/hooks/core/use-dashboard-kpi.js`
  - `dashboard/src/hooks/core/use-order-status-distribution.js`
- **Impact**: Provides interactive date range metrics and status breakdown visualizations for dashboard admins.

### ND21(v2): Rename KPI Card Component

- **Description**: Renamed `kpi-card.jsx` to `kpiCard.jsx` and updated all import statements accordingly (e.g., in `overview.jsx`).
- **Affected Files**:
  - `dashboard/src/components/core/dashboard/kpi-card.jsx` → `dashboard/src/components/core/dashboard/kpiCard.jsx`
  - Updated import path in `dashboard/src/pages/dashboard/overview.jsx`
- **Impact**: Fixes 404 errors for KPI endpoint due to missing component file and improves consistency with naming conventions.

### ND21: logo

- **Date**: 2026-08-11
- **Impact**: Dashboard Layout UI/UX
- **Description**: Replaced the collapsed sidebar background with `bg-white` (light mode) and `bg-zinc-955 / bg-zinc-950` (dark mode) to match modern premium design standards. Applied the same white and dark-zinc colors to the dashboard navigation header for layout continuity. Completely removed the user profile/role footer block from the sidebar footer.
- **Changes**:
  - Modified [`sidebar.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/ui/sidebar.jsx) (applied bg-white/bg-zinc-950 for collapsed state).
  - Modified [`header.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/dashboard/header.jsx) (applied matching header background colors).
  - Modified [`app-sidebar.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/dashboard/app-sidebar.jsx) (removed profile footer).

---

### ND20: Render Dynamic User Name and Formatted User Role in Sidebar Footer

- **Date**: 2026-08-11
- **Impact**: Dashboard App Sidebar Profile Area
- **Description**: Refactored the sidebar footer layout to display the logged-in administrator's dynamic `name` property (`user?.name` with fallback to `user?.email`) instead of hardcoding the raw email. Replaced the static `"Store Manager"` role text with the formatted, capitalized dynamic user role from the database (`user?.role`), substituting underscores with spaces.
- **Changes**:
  - Modified [`app-sidebar.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/dashboard/app-sidebar.jsx) (updated footer fields to dynamically read name and role).

---

### ND19: Customize Collapsed Sidebar Background Color to Stand Out

- **Date**: 2026-08-11
- **Impact**: Dashboard Sidebar UI/UX
- **Description**: Replaced the low-contrast golden/mustard background (`group-data-[state=collapsed]:bg-secondary`) with a dedicated noticeable color in the collapsed state. The closed sidebar now uses a distinct light-grey `bg-zinc-100` (in light mode) and a sleek dark-charcoal `bg-zinc-900` (in dark mode). This ensures that the collapsed sidebar stands out clearly from both the main content container and the expanded sidebar style, maintaining high contrast for icons.
- **Changes**:
  - Modified [`sidebar.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/ui/sidebar.jsx) (applied zinc-100/zinc-900 backgrounds for collapsed state).

---

### ND18: Add Global Custom Thin Scrollbars

- **Date**: 2026-08-11
- **Impact**: Dashboard Global Scrollbar UI/UX
- **Description**: Added custom scrollbar styles to the global base layer (`index.css`). This configures both standard-compliant scrollbars (`scrollbar-width`, `scrollbar-color`) and Webkit-based browsers (`::-webkit-scrollbar` etc.) to be modern, thin (6px), and rounded. Scrollbars now match the dashboard's layout colors (`var(--border)` and `var(--muted-foreground)`) instead of using raw default OS scrollbars.
- **Changes**:
  - Modified [`index.css`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/index.css) (custom thin scrollbar styles inside base layer).

---

### ND17: Redesign Recent Transactions to Recent Orders & Upgrade Revenue Chart UX

- **Date**: 2026-08-11
- **Impact**: Dashboard Home Overview UI/UX
- **Description**: Redesigned the "Recent Transactions" table to "Recent Orders" with a cleaner, space-efficient table structure. Customer cells now merge customer names and order dates with payment statuses in a single column to prevent horizontal scrollbar overflows on smaller dashboard viewports. Implemented premium semi-transparent status badges matching brand accents. Added a fallback for blank customer names and a "View All Orders" footer navigation. Converted the LineChart in `revenue-chart.jsx` to a premium AreaChart with linear gradients, customized hover tooltips, and fixed timezone alignment to match BD (UTC+6) aggregation offsets for daily orders representation.
- **Changes**:
  - Modified [`revenue-chart.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/dashboard/revenue-chart.jsx) (AreaChart integration, offset matching, left spacing).
  - Modified [`recent-transactions-table.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/dashboard/recent-transactions-table.jsx) (Recent Orders UI upgrade).
  - Modified [`DashboardController.js`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/backend/src/core/controllers/DashboardController.js) (timezones matching on daily aggregated metrics).

---

### ND16: Sidebar Collapsed Icon Darkening, Brands Table Pagination, Login Page Cleanup & Version Badge

- **Date**: 2026-08-11
- **Impact**: Dashboard Sidebar UX, Brands Management Table, Login Page UI
- **Description**: Applied darker icon color on collapsed sidebar state using `group-data-[collapsible=icon]:[&_svg]:text-foreground` CSS selector so nav icons are visually prominent when the sidebar is icon-only. Added client-side pagination (15 items per page) to the Brands table with Previous/Next controls, page counter, and auto-reset on search. Removed Google OAuth login section (divider, button, GoogleIcon component, `handleGoogleLogin`, `isGoogleSigningIn` state, OAuth useEffect) from the dashboard login page. Added 16px spacing above the Login button via a `pt-4` wrapper div. Injected `__APP_VERSION__` global via Vite `define` (reading from `package.json`) and rendered a fixed bottom-center version badge on the login page. Bumped dashboard version to `3.0.2`.
- **Changes**:
  - Modified `dashboard/src/components/core/dashboard/app-sidebar.jsx` (collapsed icon darkening).
  - Modified `dashboard/src/pages/dashboard/products/brands.jsx` (pagination — 15 per page, controls, search reset).
  - Modified `dashboard/src/pages/auth/login.jsx` (removed Google login, added pt-4 gap, added fixed version badge).
  - Modified `dashboard/vite.config.js` (added `__APP_VERSION__` global define from package.json).
  - Modified `dashboard/package.json` (version bump 3.0.1 → 3.0.2).

---

### ND15: Google Authenticator 2FA Integration in Login Screen

- **Date**: 2026-08-10
- **Impact**: Dashboard Login Authentication Flow & Security
- **Description**: Redesigned the LoginPage login card to feature a two-step authentication flow (Credentials entry followed by a 2FA OTP verification screen). Integrated verify2fa API hook in `auth-context.jsx` and added actions to verify codes, cancel authentication (reverting to credentials screen), and request the backend to generate and dispatch the Authenticator QR Code setup image directly to the user's email.
- **Changes**:
  - Modified `dashboard/src/pages/auth/login.jsx`.
  - Modified `dashboard/src/lib/core/auth-context.jsx`.

---

### ND14: Sidebar Auto-Expand on Click & Unbind Parent Menu Links

- **Date**: 2026-08-10
- **Impact**: Dashboard App Sidebar Interaction Flow
- **Description**: Implemented auto-expansion behavior when clicking any sidebar menu item while collapsed (`state !== "expanded"` -> `setOpen(true)`). Removed direct page link bindings from parent menus with submenus (Orders, Products, Billing, Developer Options) so clicking parent headers toggles/opens submenus and expands the sidebar without initiating unintended page navigation.
- **Changes**:
  - Modified `dashboard/src/components/core/dashboard/app-sidebar.jsx`.

---

### ND13: Password Visibility Toggle, Sidebar Logo & Accent Unification, Global Cursor Pointer

- **Date**: 2026-08-10
- **Impact**: Dashboard Login Page, App Sidebar, Global Button Cursor Styles
- **Description**: Added Eye / EyeOff password visibility toggle button to dashboard login page. Constrained sidebar header logo dimensions (`max-w-[140px]`, `h-6`) so it never overlaps the sidebar toggle X button. Updated sidebar toggle buttons to use solid primary brand accent background (`#e78a53`) with crisp black icons (`text-black`). Applied global CSS rule for `button`, `[role="button"]`, `[type="button"]` ensuring `cursor: pointer` across all interactive dashboard controls.
- **Changes**:
  - Modified `dashboard/src/pages/auth/login.jsx` (Eye/EyeOff password toggle).
  - Modified `dashboard/src/components/core/dashboard/app-sidebar.jsx` (Logo max-width constraint + solid primary toggle button with black icon).
  - Modified `dashboard/src/index.css` (Global button cursor pointer rule).

---

### ND12: Fix Unimported Lucide Icons in Sidebar, Unify Theme Accent & Implement ErrorBoundary

- **Date**: 2026-08-10
- **Impact**: Dashboard App Sidebar, Theme Color Palette & Global Error Boundary
- **Description**: Resolved `ReferenceError: Code2 is not defined` runtime crash by adding missing `Code2`, `FileCode`, and `Terminal` imports in `app-sidebar.jsx`. Unified dashboard dark theme accents to single brand amber/gold palette (`#e78a53` / `#c5a059`) in `index.css`, removing out-of-place teal/green secondary tones. Implemented `ErrorBoundary` and `RouteErrorElement` in `router.jsx` for graceful UX recovery.
- **Changes**:
  - Imported `Code2, FileCode, Terminal` in `dashboard/src/components/core/dashboard/app-sidebar.jsx`.
  - Unified theme accent variables in `dashboard/src/index.css`.
  - Created `dashboard/src/components/core/ErrorBoundary.jsx`.
  - Added `errorElement` in `dashboard/src/router.jsx`.

---

### ND11: Switch Dashboard Brand Logo to New Decantre Logo Asset

- **Date**: 2026-08-10
- **Impact**: Dashboard Sidebar, Login Header & Assets (`decantre_logo.png`)
- **Description**: Replaced old webp brand logo with the new `decantre_logo.png` asset across `DecantreLogo.jsx`, `login.jsx` header, and `app-sidebar.jsx` header.
- **Changes**:
  - Added `dashboard/src/assets/decantre_logo.png`.
  - Updated `DecantreLogo.jsx` and `app-sidebar.jsx` header.

---

### ND10: Update Dashboard Brand Logo Asset & Login Page Header

- **Date**: 2026-08-10
- **Impact**: Dashboard Assets & Login Page UI (`logo.webp`, `login.jsx`)
- **Description**: Replaced dashboard brand logo asset (`dashboard/src/assets/logo.webp`) with updated brand logo. Added `DecantreLogo` component to the Login page card header for clean branding presentation.
- **Changes**:
  - Updated `dashboard/src/assets/logo.webp`.
  - Rendered `DecantreLogo` component in `dashboard/src/pages/auth/login.jsx`.

---

### ND09: Disable Search Engine Indexing Across Dashboard

- **Date**: 2026-08-10
- **Impact**: Dashboard (SEO & Search Engine Indexing Prevention)
- **Description**: Disabled search engine crawling, indexing, and snippet caching across Google, Bing, and all web bots for the Admin Dashboard. Added strict meta tags in `index.html`, created `public/robots.txt` blocking all agents, and injected `X-Robots-Tag: noindex, nofollow, noarchive, nosnippet` in Nginx headers.
- **Changes**:
  - Updated `dashboard/index.html` with `noindex, nofollow` meta tags for robots and googlebot.
  - Created `dashboard/public/robots.txt`.
  - Updated `dashboard/nginx.conf` and VPS Nginx site configs with `X-Robots-Tag` headers.

---

### ND08: Add Request Source Origin Detection and Filtering (Dashboard vs Frontend)

- **Date**: 2026-08-10
- **Impact**: Dashboard System Logs UI (`systemLogs.jsx`, `app-sidebar.jsx`)
- **Description**: Added request source origin detection to distinguish API calls originating from the Admin Dashboard vs Frontend Storefront. Added interactive Source Filter dropdown in System Logs UI allowing real-time filtering by origin (All, Dashboard Only, Frontend Only, External API). Updated Lucide icon imports (`Code2`, `FileCode`, `Terminal`) in `app-sidebar.jsx`.
- **Changes**:
  - Added `sourceFilter` dropdown state and badge rendering in `systemLogs.jsx`.
  - Updated icons in `app-sidebar.jsx`.

---

### ND07: Add Developer Swagger API Docs and Realtime System Logs pages

- **Date**: 2026-08-10
- **Impact**: Dashboard (Developer Tools, Swagger API Specs, Realtime Log Telemetry, Hardcoded Access Control)
- **Description**: Added dedicated developer options in the dashboard including Swagger UI API Documentation (`/dashboard/developer/v1/doc.html`) and Realtime System Logs Viewer (`/dashboard/developer/logs`). Hardcoded access restriction on both frontend and backend to `ikramul.web@gmail.com`.
- **Changes**:
  - Created `apiDocs.jsx` and `systemLogs.jsx` under `dashboard/src/pages/dashboard/developer/`.
  - Registered routes in `dashboard/src/router.jsx`.
  - Conditionally rendered Developer Options in `app-sidebar.jsx` for `ikramul.web@gmail.com`.
  - Created `DeveloperRoute.js` in backend with `verifyDeveloperAccess` middleware.

---

### ND06: Fix Product Edit Page Loading Error and Add Image Upload Validation

- **Date**: 2026-08-08
- **Impact**: Dashboard (Product Edit, Product Add, Image Validation, Routing)
- **Description**: Fixed critical bug where clicking any product to edit showed "Failed to load product details." error — caused by using a broken `window.location.pathname` hack to extract the product ID instead of React Router v6's `useParams()` hook. Also fixed `imageUrl` not being saved to the database when uploading a new image during product create/update. Added strict frontend validation to block form submission if no product image is selected or already uploaded.
- **Changes**:
  - Replaced `window.location.pathname.split('/').pop()` with `useParams()` in `productDetails.jsx` — resolves product edit page 404 error.
  - Fixed `imageUrl: finalMainImageUrl || undefined` → `imageUrl: finalMainImageUrl || ""` in `newProduct.jsx` and `productDetails.jsx` so uploaded image URL is always sent to the backend.
  - Added `thumbnailUrl` to product create body in `newProduct.jsx`.
  - Added frontend image required validation: if no image file or existing URL, form submit is blocked with a clear toast error message.

---

### ND05: Replace Full Page Window Reloads with React Router Client Side Navigation

- **Date**: 2026-08-08
- **Impact**: Dashboard (Product Add & Edit, SPA Routing & User Experience)
- **Description**: Replaced legacy `window.location.href = "/dashboard/products"` calls with React Router `useNavigate()` hook (`navigate("/dashboard/products")`) and `<Link to="/dashboard/products">` across [`newProduct.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/newProduct.jsx) and [`productDetails.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/productDetails.jsx). This eliminates full browser reloads when creating or updating products while invalidating React Query cache to keep the product list seamlessly updated.
- **Changes**:
  - Replaced `window.location.href` redirect with `useNavigate()` in [`newProduct.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/newProduct.jsx) and [`productDetails.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/productDetails.jsx).
  - Replaced standard HTML anchor tags with `<Link>` components for back navigation.

---

### ND04: Update Brand Selection Labels to Brand and Sub Brand and Add Attribute Values Sorting

- **Date**: 2026-08-08
- **Impact**: Dashboard (Product Add & Edit, Attribute Management UI, Brand Labels)
- **Description**: Updated dropdown labels in 2-step brand selection from "Parent Brand" to "Brand" and "Brand Name" to "Sub Brand" across product add (`newProduct.jsx`) and edit (`productDetails.jsx`) pages. Fixed DID/slug matching in `<SelectValue>` display so canonical brand name displays instead of raw MongoDB IDs. Added natural numeric sorting for attribute values in `attributes.jsx`, `newProduct.jsx`, and `productDetails.jsx`.
- **Changes**:
  - Updated dropdown labels and placeholders to "Brand" and "Sub Brand" in [`productDetails.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/productDetails.jsx) and [`newProduct.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/newProduct.jsx).
  - Resolved DID vs slug matching issue in Select components to display human-readable brand names.
  - Added natural numeric attribute sorting in [`attributes.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/attributes.jsx), [`newProduct.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/newProduct.jsx), and [`productDetails.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/productDetails.jsx).

---

### ND03: Refactor OrderDetails Component with Dedicated Helper and Implement Image Upload Fix & 2MB Validation

- **Date**: 2026-08-08
- **Impact**: Dashboard (Order Details, Helper Modularization, Product Edit & Add Image Validation)
- **Description**: Extracted complex order calculation, badge rendering, phone formatting, and payload building logic from `orderDetails.jsx` into a dedicated arrow-function helper `orderDetailsHelper.jsx`. Fixed product edit image URL update persistence issue in `productDetails.jsx`. Added 2MB maximum image size validation with in-place red borders and error messages ("Maximum size exceeded. Please upload a different image.") across product add and edit pages (`newProduct.jsx` and `productDetails.jsx`).
- **Changes**:
  - Extracted pure arrow helper functions into [`orderDetailsHelper.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/utils/core/orderDetailsHelper.jsx).
  - Refactored [`orderDetails.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/orders/orderDetails.jsx) to use modular helper methods.
  - Fixed image URL update persistence and payload JSON property handling in [`productDetails.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/productDetails.jsx).
  - Added 2MB file size check and in-place error UI to [`productDetails.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/productDetails.jsx) and [`newProduct.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/dashboard/products/newProduct.jsx).

---

### ND02: Fix Dashboard Tailwind CSS v4 Theme Inline and Missing ThemeProvider Context

- **Date**: 2026-08-08
- **Impact**: Dashboard (Styling, Dark Mode, Theme Context, Vite Build)
- **Description**: Resolved issues causing dashboard CSS styling to fail to render properly. Configured `@theme inline` in `src/index.css` so Tailwind CSS v4 dynamically evaluates color and theme variables defined in `:root` and `.dark`. Added `ThemeProvider` wrapper from `next-themes` inside `ReactQueryProvider` in `providers.jsx` so theme context and header theme toggle work properly. Reordered Vite plugins in `vite.config.js` to `[react(), tailwindcss()]`.
- **Changes**:
  - Updated `@theme` to `@theme inline` in [`index.css`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/index.css).
  - Wrapped `ThemeProvider` in [`providers.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/providers.jsx) and updated component syntax to arrow function.
  - Updated `ThemeProvider` in [`theme-provider.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/theme-provider.jsx) to arrow function syntax.
  - Adjusted plugin order in [`vite.config.js`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/vite.config.js).

---

### ND01: Restructure Dashboard to Standard React SPA Architecture with React Router

- **Date**: 2026-08-07
- **Impact**: Dashboard (Architecture, Routing, Layouts, Pages, File Naming)
- **Description**: Restructured the Vite-based dashboard application from a Next.js App Router style folder structure (`src/app/(core)/...`) to standard React SPA best practices (`src/pages/` and `src/layouts/`). Replaced manual page reloads and `window.location` routing with `react-router-dom` (`createBrowserRouter`, `RouterProvider`, `useNavigate`, `useLocation`, `<Link>`). Added `ReactQueryProvider` wrapper in `App.jsx` to resolve the uncaught auth context exception that was rendering a blank screen. Converted all page component filenames and imports to `camelCase`.
- **Changes**:
  - Moved and renamed layout component to [`dashboardLayout.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/layouts/dashboardLayout.jsx).
  - Reorganized pages under `src/pages/auth/`, `src/pages/dashboard/`, `src/pages/dashboard/products/`, `src/pages/dashboard/orders/`, and `src/pages/dashboard/billing/` with camelCase filenames.
  - Configured centralized router in [`router.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/router.jsx).
  - Configured `RouterProvider` and `ReactQueryProvider` in [`App.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/App.jsx).
  - Refactored navigation links in [`app-sidebar.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/dashboard/app-sidebar.jsx) to use `<Link>` and `useLocation`.
  - Updated redirect handling in [`auth-guard.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/components/core/auth-guard.jsx) and [`login.jsx`](file:///c:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/src/pages/auth/login.jsx) to use `useNavigate`.
