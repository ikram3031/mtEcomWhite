# Antigravity Dashboard Change Logs (AD04)

### AD04: Update VITE_API_BASE_URL to point to live backend

* **Date**: 2026-08-09
* **Impact**: Dashboard (`.env`, `.env.local`, `docker-compose.live.yml`)
* **Description**: The dashboard API base URL was pointing to `server.decantrebd.com`, which caused API requests to fail. Also, `docker-compose.live.yml` was passing `NEXT_PUBLIC_API_BASE_URL` instead of `VITE_API_BASE_URL`. Updated all config files to properly use `https://service.decantrebd.com` as the `VITE_API_BASE_URL`.
* **Changes**:
  - Modified [`dashboard/.env`](file:///C:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/.env) and [`dashboard/.env.local`](file:///C:/Users/mdikr/Documents/CODE/Decantre_Fullstack/dashboard/.env.local) to point to `service.decantrebd.com`.
  - Modified [`docker-compose.live.yml`](file:///C:/Users/mdikr/Documents/CODE/Decantre_Fullstack/docker-compose.live.yml) to use the correct `VITE_API_BASE_URL` argument for the dashboard service build.
