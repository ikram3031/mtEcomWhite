# Keep branch-specific backend Dockerfile, docker-compose, nginx, and env configs intact during merges
Dockerfile* merge=ours
.dockerignore merge=ours
docker-compose*.yml merge=ours
nginx*.conf merge=ours
.env* merge=ours

# Keep Live-branch email templates intact — never overwrite with Decantre/master versions
src/templates/*.js merge=ours
