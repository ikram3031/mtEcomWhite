# [AB-67] Split Monorepo & Remove Storefront Frontend

## Summary
- Removed the customer storefront `frontend/` directory from the `Live` branch.
- The storefront frontend is split into a separate repository to run independently on VPS deployments.

## Affected Files
- [DELETED] `frontend/` (all files and folders inside)

## Business Impact
- Separates customer storefront code from backend/dashboard management core services.
- Simplifies independent deployment pipelines for the storefront and the API core.
